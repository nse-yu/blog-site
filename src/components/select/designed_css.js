import { css } from "@emotion/react"

const select_wrapper = css`
    border:3px solid black;
    border-radius:8px;
    padding:3px;
    height:45px;
`
const select_wrapper___hidden = css`
`
const select_item = css`
    border:2px solid black;
    border-radius:10px;
    padding:0.2rem;
    cursor:pointer;
`

export const designedSet = {
    select_wrapper,
    select_wrapper___hidden,
    select_item
}